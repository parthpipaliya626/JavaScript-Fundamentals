// storing objects as references
// const variable use that time last value is define all time 
// and const in a not same class created you have to try then you show error

const user = {
    name: "Parth"
};

user.name = "Raju";  

alert(user.name);
