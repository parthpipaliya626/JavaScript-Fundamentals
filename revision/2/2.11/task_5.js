console.log(null || 2 && 3 || 4);      // 3

// 2 && 3 = 3 aavse then && method last output aapse 
// then || method ma first value output aavse.

// so  3 output aavse

// Now the result is the first truthy value: 3.
