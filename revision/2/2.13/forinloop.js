// The "for..in" loop

const { keyIn } = require("readline-sync");

let user = {
    name : "Parth",
    age : 30,
    isAdmin : true
};

for (key in user ) {
    console.log(key);
}

















